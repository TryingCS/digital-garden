---
{"dg-publish":true,"permalink":"/🛠️/python📁/syntax/","dg-note-properties":{}}
---

```python
int()// to convert a string to an integer 
```


```python
color = input("Enter a color: ")          
 # getting input 
print("Roses are " + color)                        // printing value to console 
```

```python
i = 5
# Without f-string:
print("Step " + str(i)) 

# With f-string (much cleaner):
print(f"Step {i}") 
```
***

- After a keyword (`if`, `for`, `def`) -> I **"Start of indented block"**.
- Inside `{}` -> It means **"Key maps to Value"**.
***
- `if/else`: "Check this condition. If true, do this."
- `try/except`: "Just try doing this. If it crashes, do this instead."
-"Catching" means stopping a crash from killing your entire program.
***
`input()`: Whatever  typed,  saved as string.
`int()`
